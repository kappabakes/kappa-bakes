Drop one photo per flavour here, named to match the id in lib/config.ts:

  plain-jane.jpg
  special-k.jpg
  chocolate-one.jpg
  berry-bliss.jpg

Square crops, around 600x600, under 200KB each. Shoot them on the same
surface in the same light — a consistent set reads as a menu, a mixed set
reads as a scrapbook.
